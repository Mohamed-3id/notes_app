name: section_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.0.2
  path: ^1.8.0
  shared_preferences:
  provider:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons:

flutter_icons:
  android: true
  ios: true
  image_path: "Assets/Images/logo.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "Assets/Images/logo-android.png"

flutter:

  uses-material-design: true
  
  assets:
    - Assets/Images/